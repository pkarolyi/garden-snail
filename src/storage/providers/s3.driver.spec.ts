import { describe, it } from "vitest";

describe("S3StorageDriver", () => {
  describe("TODO", () => {
    it("aws-sdk-client-mock is a pain to work with and I", () => {});
    it("don't have the patience currently to make it work", () => {});
  });
});
